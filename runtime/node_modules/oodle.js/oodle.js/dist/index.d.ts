import koffi from 'koffi';

/**
 * Oodle compression level. Use this to fine tune your speed to compression ratio.
 *
 * ### Recommended
 * - Use {@link Fast} for testing
 * - Use {@link Optimal} for production
 * - Use {@link Normal} to get a feel for the compression speed of your data to choose a compression level
 *
 * ### Info
 * Higher numerical value **=** slower and higher compression `(smaller size)`
 *
 * Smaller numerical value **=** faster and lower compression `(bigger size)`
 *
 * Compression Level does not affect decode speed, only encode speed.
 *
 * {@link HyperFast} compression level are only available in {@link OodleCompressor.Kraken}, {@link OodleCompressor.Mermaid} and {@link OodleCompressor.Selkie}
 */
declare enum OodleCompressionLevel {
    /**
     * No compression, copies the raw bytes.
     */
    None = 0,
    /**
     * Fastest but most minimal compression.
     *
     * Faster than {@link HyperFast3}
     *
     * Same as {@link HyperFast4}
     */
    Min = 1,
    /**
     * Fastest but most minimal compression.
     *
     * Faster than {@link HyperFast3}
     *
     * Same as {@link HyperFast4}
     */
    Fastest = 2,
    /**
     * Fastest but most minimal compression.
     *
     * Faster than {@link HyperFast3}
     */
    HyperFast4 = -4,
    /**
     * Extremely fast but very minimal compression.
     *
     * Faster than {@link HyperFast2}
     */
    HyperFast3 = -3,
    /**
     * Hyper fast but minimal compression.
     *
     * Faster than {@link HyperFast} and {@link HyperFast1}
     */
    HyperFast2 = -2,
    /**
     * Hyper fast but extremely low compression.
     *
     * Faster than {@link SuperFast}
     *
     * Same as {@link HyperFast1}
     */
    HyperFast = -1,
    HyperFast1 = -1,
    /**
     * Super fast but very low compression.
     *
     * Faster than {@link VeryFast}
     */
    SuperFast = 1,
    /**
     * Very fast but low compression.
     *
     * Faster than {@link Fast}
     */
    VeryFast = 2,
    /**
     * Fast and decent compression.
     *
     * Recommended for daily use by [Epic Games](https://dev.epicgames.com/documentation/en-us/unreal-engine/oodle-data).
     *
     * Faster than {@link Normal}
     */
    Fast = 3,
    /**
     * Decent speed and medium compression.
     */
    Normal = 4,
    /**
     * Fast optimal encoding.
     *
     * Slower than {@link Optimal} and {@link Optimal2}
     */
    Optimal1 = 5,
    /**
     * Optimal encoding.
     *
     * Recommended baseline optimal encoder by [Epic Games](https://dev.epicgames.com/documentation/en-us/unreal-engine/oodle-data).
     *
     * Same as {@link Optimal2}
     */
    Optimal = 6,
    /**
     * Optimal encoding.
     *
     * Recommended baseline optimal encoder by [Epic Games](https://dev.epicgames.com/documentation/en-us/unreal-engine/oodle-data).
     */
    Optimal2 = 6,
    /**
     * Slower optimal encoding.
     *
     * Slower than {@link Optimal} and {@link Optimal2}
     */
    Optimal3 = 7,
    /**
     * Very slow optimal encoding.
     *
     * Slower than {@link Optimal3}
     */
    Optimal4 = 8,
    /**
     * Slowest encoding but maximum compression.
     *
     * Slower than {@link Optimal4}
     *
     * Same as {@link Optimal5}
     */
    Max = 9,
    /**
     * Slowest encoding but maximum compression.
     *
     * Slower than {@link Optimal4}
     *
     * Same as {@link Optimal5}
     */
    Slowest = 10,
    /**
     * Slowest encoding but maximum compression.
     *
     * Slower than {@link Optimal4}
     */
    Optimal5 = 9
}
/**
 * The compression algorithm used by Oodle.
 *
 * ### Recommended
 * - {@link Kraken} for normal use.
 * - {@link Mermaid} when CPU power is limited.
 */
declare enum OodleCompressor {
    /**
     * No compression, copies the raw bytes.
     */
    None = 3,
    /**
     * @deprecated as of Oodle `2.9.0`. Only use with your own provided **older** Oodle lib.
     */
    LZH = 0,
    /**
     * @deprecated as of Oodle `2.9.0`. Only use with your own provided **older** Oodle lib.
     */
    LZHLW = 1,
    /**
     * @deprecated as of Oodle `2.9.0`. Only use with your own provided **older** Oodle lib.
     */
    LZNIB = 2,
    /**
     * @deprecated as of Oodle `2.9.0`. Only use with your own provided **older** Oodle lib.
     */
    LZB16 = 4,
    /**
     * @deprecated as of Oodle `2.9.0`. Only use with your own provided **older** Oodle lib.
     */
    LZBLW = 5,
    /**
     * @deprecated as of Oodle `2.9.0`. Only use with your own provided **older** Oodle lib.
     */
    LZA = 6,
    /**
     * @deprecated as of Oodle `2.9.0`. Only use with your own provided **older** Oodle lib.
     */
    LZNA = 7,
    /**
     * High compression with good decode speed.
     *
     * Recommended to use as your default option.
     */
    Kraken = 8,
    /**
     * Less compression and faster decode speed.
     *
     * Recommended to use when CPU power is limited.
     *
     * Faster than {@link Kraken}
     */
    Mermaid = 9,
    /**
     * @deprecated as of Oodle `2.9.0`. Only use with your own provided **older** Oodle lib.
     */
    Bitknit = 10,
    /**
     * Least compression and fastest decode speed.
     *
     * Fast than {@link Mermaid}
     */
    Selkie = 11,
    /**
     * A meta compressor that uses {@link Kraken}, {@link Mermaid}, {@link Selkie} and {@link Leviathan} under the hood.
     *
     * It automatically chooses one of those compressors to encode each block. This results in a mix of each compressor in the final result, giving a better average speed to compression ratio.
     */
    Hydra = 12,
    /**
     * Most compression but slower decode speed.
     *
     * Slower than {@link Kraken}
     */
    Leviathan = 13
}
/**
 * Log level used by Oodle used to determine what info to log.
 *
 * @deprecated Unused by the new compressors. Only use with your own provided **older** Oodle lib.
 */
declare enum OodleVerbosity {
    None = 0,
    Min = 1,
    Some = 2,
    Max = 3
}
/**
 * Enable extra safety checks
 *
 * Recommended to use {@link Yes}
 */
declare enum OodleFuzzSafe {
    No = 0,
    Yes = 1
}
/**
 * Check CRC for errors
 *
 * Recommended to use {@link Yes}
 */
declare enum OodleCheckCRC {
    No = 0,
    Yes = 1
}
declare enum OodleDecodeThreadPhase {
    Phase1 = 1,
    Phase2 = 2,
    All = 3,
    Unthreaded = 3
}
interface NativeLib {
    Decompress(src_buf: Buffer, src_len: number, dst: Buffer, dst_size: number, fuzz: OodleFuzzSafe, crc: OodleCheckCRC, verbose: OodleVerbosity, dst_base: null, dst_base_size: number, cb: null, cb_ctx: null, scratch: null, scratch_size: number, threadPhase: OodleDecodeThreadPhase): number;
    Compress(compressor: OodleCompressor, src_buf: Buffer, src_len: number, dst_buf: Buffer, level: OodleCompressionLevel, opts: null, dictionary: null, lrm: null, scratch: null, scratch_size: number): number;
    GetAllChunksCompressor(src_buf: Buffer, src_len: number, raw_len: number): number;
    GetDecodeBufferSize(compressor: OodleCompressor, raw_size: number, corruption_possible: boolean | number): number;
    GetCompressedBufferSizeNeeded(compressor: OodleCompressor, raw_size: number): number;
}

declare class Oodle {
    _lib: koffi.IKoffiLib;
    _Decompress: NativeLib["Decompress"];
    _Compress: NativeLib["Compress"];
    _GetAllChunksCompressor: NativeLib["GetAllChunksCompressor"];
    _GetDecodeBufferSize: NativeLib["GetDecodeBufferSize"];
    _GetCompressedBufferSizeNeeded: NativeLib["GetCompressedBufferSizeNeeded"];
    private constructor();
    /**
     * Creates an Oodle instance.
     *
     * Downloads oodle lib file according to the current os and architecture or uses the provided path to oodle lib file.
     *
     * @param pathOrClearCache - path to the oodle lib file to use, or a boolean to indicate whether to clear download cache
     *
     * @returns {Oodle}
     */
    static Create(pathOrClearCache?: string | boolean): Promise<Oodle>;
    /**
     * Compresses a buffer using the Oodle compression library.
     * Uses `OodleLZ_Compress`.
     *
     * @param src - Source buffer options
     * @param src.buffer - The source buffer containing compressed data.
     * @param src.size - The number of bytes to read from the source buffer.
     * @param src.offset - The byte offset in {@link src.buffer} where compressed data begins.
     * @param compressor - The compression algorithm to use.
     * See {@link OodleCompressor}. Defaults to {@link OodleCompressor.Kraken}.
     * @param level - The compression level controlling speed versus compression ratio.
     * See {@link OodleCompressionLevel}. Defaults to {@link OodleCompressionLevel.Optimal}.
     *
     * @returns A buffer containing the compressed data.
     *
     * @throws {OodleError} Throws if compression fails.
     */
    compress({ buffer, size, offset, }: {
        buffer: Buffer;
        size?: number;
        offset?: number;
    }, compressor?: OodleCompressor, level?: OodleCompressionLevel): Buffer<ArrayBuffer>;
    /**
     * Decompresses Oodle-compressed data into a destination buffer.
     * Uses `OodleLZ_Decompress`.
     *
     * @param src - Source buffer options
     * @param src.buffer - The source buffer containing compressed data.
     * @param src.size - The number of bytes to read from the source buffer.
     * @param src.offset - The byte offset in {@link src.buffer} where compressed data begins.
     * @param originalSize - The original uncompressed size of the data.
     * @param options - Optional decompression settings.
     * @param options.fuzzSafe - Enables fuzz-safe decompression behavior.
     * See {@link OodleFuzzSafe}. Defaults to {@link OodleFuzzSafe.Yes}.
     * @param options.checkCRC - Enables CRC validation during decompression.
     * See {@link OodleCheckCRC}. Defaults to {@link OodleCheckCRC.No}.
     * @param options.verbosity - Controls Oodle decompression logging verbosity.
     * See {@link OodleVerbosity}. Defaults to {@link OodleVerbosity.None}.
     * @param options.decodeThreadPhase - Controls the decode threading phase.
     * See {@link OodleDecodeThreadPhase}. Defaults to {@link OodleDecodeThreadPhase.Unthreaded}.
     *
     * @returns A buffer containing the decompressed data.
     *
     * @throws {OodleError} Throws if decompression fails or the destination buffer is too small.
     */
    decompress({ buffer, size, offset, }: {
        buffer: Buffer;
        size?: number;
        offset?: number;
    }, originalSize: number, { fuzzSafe, checkCRC, verbosity, decodeThreadPhase, }?: {
        fuzzSafe?: OodleFuzzSafe | undefined;
        checkCRC?: OodleCheckCRC | undefined;
        verbosity?: OodleVerbosity | undefined;
        decodeThreadPhase?: OodleDecodeThreadPhase | undefined;
    }): Buffer<ArrayBuffer>;
    /**
     * Detects the Oodle compressor used in a compressed buffer.
     * Uses `OodleLZ_GetAllChunksCompressor`.
     *
     * This inspects the provided compressed data and returns the
     * compressor type that was used to encode it.
     *
     * @param src - The compressed buffer.
     * @param srcSize - Optional number of bytes to read from the buffer.
     * If omitted, the full buffer length is used.
     * @param srcOffset - Optional byte offset where compressed data begins.
     *
     * @returns The detected {@link OodleCompressor} type.
     *
     * @throws {OodleError} Throws if the buffer is empty or invalid.
     */
    getCompressor(src: Buffer, srcSize?: number, srcOffset?: number): OodleCompressor;
    /**
     * Estimates the maximum possible compressed size for a given input size
     * using a specific Oodle compressor.
     * Uses `OodleLZ_GetCompressedBufferSizeNeeded`.
     *
     * This is useful for preallocating output buffers before compression.
     *
     * @param srcSize - The size (in bytes) of the uncompressed input data.
     * Must be greater than 0.
     * @param compressor - The Oodle compressor to use.
     * See {@link OodleCompressor}.
     *
     * @returns The maximum number of bytes that may be required
     * to store the compressed output.
     *
     * @throws {OodleError} Throws if `srcSize` is invalid or
     * if the compressor is not recognized.
     */
    maxCompressedSize(srcSize: number, compressor: OodleCompressor): number;
    /**
     * Calculates the minimum buffer size required to safely decompress
     * Oodle-compressed data.
     * Uses `OodleLZ_GetDecodeBufferSize`.
     *
     * This helps ensure the destination buffer is large enough before
     * calling `decompress()`.
     *
     * @param srcOrSize - Either a compressed buffer or its size in bytes.
     * If a Buffer is provided, its length is used automatically.
     * @param compressor - The Oodle compressor used for encoding.
     * See {@link OodleCompressor}.
     * @param corruptionPossible - Whether to account for potential
     * corrupted input data. Defaults to `false`.
     *
     * @returns The minimum required decode buffer size in bytes.
     *
     * @throws {OodleError} Throws if the input size is invalid or
     * if the compressor is not recognized.
     */
    minDecodeSize(srcOrSize: Buffer | number, compressor: OodleCompressor, corruptionPossible?: boolean): number;
    private crop;
}
declare class OodleError extends Error {
    code: string;
    constructor(message: string, code: string);
    isOodleError(): boolean;
}

export { Oodle, OodleCheckCRC, OodleCompressionLevel, OodleCompressor, OodleDecodeThreadPhase, OodleError, OodleFuzzSafe, Oodle as default };
